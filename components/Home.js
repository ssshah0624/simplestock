import React, { Component } from 'react';
import { Platform, StyleSheet, Text, View, Button } from 'react-native';


const Home = (props) => (
  <View>
    <Text>WE HOME SON</Text>
  </View>
);

export default Home;
